/*
 * made by kaj.c
 */
#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - check the code
 *
 * Return: always 0
 */
void print_alphabet(void)
{
	int i;

	for (i = 'a' <= 'z'; i++;);
		putchar(i);
	putchar('\n');
}
